# Implement Prompt Template

## Goal
<insert concise objective for implementing the change>

## Inputs
- Target files/modules: <insert file paths or modules to edit>
- Existing behavior: <insert current behavior summary>
- Acceptance criteria: <insert measurable success criteria>

## Steps
1. <insert step to inspect specific file and location>
2. <insert step to modify code with exact identifiers>
3. <insert step to update related configuration or docs>
4. <insert step to validate behavior locally>

## Output Format
- Summary: <insert 1–3 sentence summary of changes>
- Changes:
  - <insert bullet with file path and change description>
  - <insert bullet with file path and change description>
- Tests/Checks:
  - <insert exact command, e.g., "<insert exact make target>">
- Citations: <insert file/command citations (e.g., "src/app.py", "make test")>
