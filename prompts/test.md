# Test Prompt Template

## Goal
<insert concise objective for testing>

## Inputs
- Test scope: <insert components or paths to validate>
- Commands available: <insert specific commands or make targets>
- Environment notes: <insert relevant env/setup details>

## Steps
1. <insert step to identify relevant test targets>
2. <insert step to run unit/integration tests>
3. <insert step to capture and summarize results>
4. <insert step to note failures or warnings>

## Output Format
- Summary: <insert 1–3 sentence testing summary>
- Commands Run:
  - <insert exact command, e.g., "<insert exact make target>">
  - <insert exact command, e.g., "<insert docker compose service name>">
- Results:
  - <insert pass/fail/warn with brief reason>
- Citations: <insert file/command citations (e.g., "Makefile", "pytest -q")>
