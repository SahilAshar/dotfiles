# Deploy Prompt Template

## Goal
<insert concise objective for deployment>

## Inputs
- Deployment target: <insert environment or platform>
- Services/components: <insert docker compose service name or target>
- Required credentials/vars: <insert required secrets or env vars>

## Steps
1. <insert step to verify build artifacts or version>
2. <insert step to run deployment command>
3. <insert step to verify deployment health>
4. <insert step to document rollback or cleanup>

## Output Format
- Summary: <insert 1–3 sentence deployment summary>
- Commands Run:
  - <insert exact command, e.g., "<insert exact make target>">
  - <insert exact command, e.g., "<insert docker compose service name>">
- Verification:
  - <insert health check endpoint or validation>
- Citations: <insert file/command citations (e.g., "deploy.sh", "kubectl apply -f")>
